{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The status window listens for state updates and calls back into the shell.",
  "windows": ["status"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-set-size"
  ]
}
